---
title: Running Repositories and Applications in the NRP
---

# Running Applications in the NRP

This documentation describes running Kubernetes containers in the National Repository Platform (NRP).

The intended audience is:

- repository administrators who run their own repository implementations in the infrastructure,
- administrators who run NRP-related applications in the infrastructure.

Kindly note that the NRP is not intended for generic data computation and/or processing, the applications need to be tightly related to the purpose of the platform.

Environment for running containerised applications in the NRP is built on a managed Kubernetes container platform, accessible either through the Rancher web interface or the native Kubernetes API.

## Getting Access

To utilize the data processing infrastructure, you need an active NRP account (in a suitable Virtual Organisation) and an assigned Kubernetes project.

To request a Kubernetes project, please contact us at [support@eosc.cz](mailto:support@eosc.cz) and provide the following information:

- Preferred name for the project
- Required resource quotas (CPU and Memory)
- List of users or user group(s) to be granted access
- Also note that it is a NRP service

## Using the Infrastructure

Once your project is successfully created, you can deploy your application.

The NRP Kubernetes environment is based on the same infrastructure as used in [CERIT-SC](https://www.cerit-sc.cz/), and therefore, you can refer to CERIT-SC documentation for detailed guidance (including the web interface, you can access NRP Kubernetes directly through the Rancher Web UI instance documented below).

### Accessing the Platform

- **Rancher Web UI**: Use the [Rancher documentation](https://docs.cerit.io/en/docs/rancher/rancher) to manage your Kubernetes resources via the user-friendly web interface.
- **kubectl CLI**: Follow the [kubectl documentation](https://docs.cerit.io/en/docs/kubernetes/kubectl) to configure your Kubernetes context and interact directly with the cluster using the command-line interface.

### Additional Resources

- **PostgreSQL**: If your application or repository requires PostgreSQL, refer to the [Postgres Operator documentation](https://docs.cerit.io/en/docs/operators/postgres-cnpg) for setup and management instructions. We strongly recommend using the Postgres Operator instead of running PostgreSQL manually.
